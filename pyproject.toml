[project]
name = "entropic-sampling"
version = "0.1.0"
description = "Future-Entropy Sampler (Count Bayesie)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gguf>=0.19.0",
    "jinja2>=3.1.6",
    "llama-cpp-python>=0.3.34",
    "torch>=2.13.0",
    "transformers>=5.14.1",
]

